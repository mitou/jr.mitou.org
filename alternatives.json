---
---

[ {% for alt in site.data.alternatives %}
  {
    "id"          : "{{ alt.id }}",
    "url"         : "{{ alt.url }}",
    "title"       : "{{ alt.title }}",
    "description" : "{{ alt.description }}"
  }{% unless forloop.last %},{% endunless %}{% endfor %}
]
